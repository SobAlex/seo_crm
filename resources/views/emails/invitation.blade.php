<x-mail::message>
    # Приглашение в компанию

    Вас пригласили в компанию **{{ $teamName }}** в качестве **{{ $role }}**.

    Перейдите по ссылке, чтобы зарегистрироваться:

    <x-mail::button :url="$url">
        Принять приглашение
    </x-mail::button>

    Ссылка действительна 7 дней.

    С уважением,<br>
    {{ config('app.name') }}
</x-mail::message>
